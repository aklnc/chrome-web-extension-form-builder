{
  "dependencies": {
    "chrome-types": "^0.1.219"
  },
  "devDependencies": {
    "copyfiles": "^2.4.1",
    "rimraf": "^5.0.1"
  },
  "scripts": {
    "clean": "rimraf src/",
    "copy-files": "copyfiles -u 1 dev/**/*.html dev/**/*.json dev/**/*.css src/",
    "build": "npm clean && tsc && yarn copy-files"
  }
}
